\section{Execution Plan}

\subsection{Schedule}

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{@{}L{1.9cm}L{4.2cm}X@{}}
\toprule
\textbf{Phase} & \textbf{Stage} & \textbf{Work} \\
\midrule
Phase 1 & Chunking &
Boundary detectors, weighted fusion, the three chunking modes, duration bounds.
Validated on cut-free footage before anything was built on top. \\
Phase 2 & Analysis &
Scene, people, object, text and speech analyzers; frame sampling; detector gating;
the registry that makes them interchangeable. \\
Phase 3 & Indexing and retrieval &
Embedding, named vectors, the filter set, detail levels, search endpoint. \\
Phase 4 & Aggregation &
Video-level output, dependency ordering, caching, entity and object linking. \\
Phase 5 & Question answering &
Routing questions to the right aggregates, answer synthesis with citations. \\
Phase 6 & Interface &
Workspace, upload and status, agent chat, clip panel, playback. \\
Phase 7 & Hardening and documentation &
Acceptance tests on a live server, error handling, API documentation, this report. \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Distribution of Work}

The project is built by two people. The split follows the pipeline: everything from a
recording arriving to it being chunked and stored on one side, everything from a chunk
being analysed to a question being answered on the other.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{@{}L{4.2cm}X@{}}
\toprule
\textbf{Member} & \textbf{Responsibility} \\
\midrule
Avinash Anish &
\textbf{Frontend and ingestion.} The whole interface --- workspace, upload and status,
agent chat, clip panel and playback --- together with the agent tool layer. On the
pipeline side, ingestion: source handling, storage and caching, content-addressed
video identity, the boundary detectors, weighted fusion and the three chunking modes,
and the background job and progress reporting behind them. \\
Vaivaswat Verma &
\textbf{Backend and retrieval.} Everything downstream of chunking: the analyzers and
their frame sampling and detector gating, embedding and the vector store, filters and
detail levels, the video-level aggregators including entity and object linking, and
question routing and answer synthesis. Owns the HTTP API these are exposed through. \\
\midrule
Shared &
Architecture and design decisions, acceptance testing against a live server,
deployment, and documentation. \\
\bottomrule
\end{tabularx}
\end{table}
